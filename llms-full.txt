# Internacia Datasets

> Machine-friendly reference data for countries, intergovernmental organizations (intblocks),
> and organization block types. Part of the Dateno open-source project.
> Licensed CC-BY-4.0 (data and documentation). Code is MIT.
> Version and schema: see data/datasets/*.manifest.json
> 中文索引: llms.zh.txt

## What this is

- **Countries** (256 records): ISO codes, geography, demographics, World Bank classifications, Wikidata links, multilingual names, stable attributes (`car_side`, writing systems/directions, DVD region, broadcast/legal systems, rail gauges)
- **Intblocks** (1037 records): UN agencies, trade blocs, regional orgs, treaties, sports federations, named geographic groupings, etc.
- **Blocktypes** (78 records): taxonomy for intblock categories

This is **reference data for joins and enrichment**, not a socioeconomic profile dataset.
Do NOT expect HDI, GDP, government type, internet penetration, or similar indicators here.

## Preferred access (do not parse source YAML unless editing)

- DuckDB: data/datasets/internacia.duckdb
  - Tables: countries, intblocks, blocktypes, memberships, _meta
- Parquet: data/datasets/{countries,intblocks,blocktypes}.parquet
- Membership edges (flattened): data/datasets/memberships.parquet / memberships.csv.zst / DuckDB table memberships
  - Columns: intblock_id, country_code, include_type, status, joined, left
- Plain JSONL: data/datasets/{countries,intblocks,blocktypes}.jsonl
- Compressed JSONL: data/datasets/{countries,intblocks,blocktypes}.jsonl.zst (decompress with zstd -d)
- Python SDK: https://github.com/datenoio/internacia-python
- REST API: https://github.com/datenoio/internacia-api

Do NOT read data/_legacy/ — obsolete pre-1.0 snapshots kept for historical reference only.

## Schemas and metadata

- JSON Schema (source YAML): data/schemas/countries.schema.json, data/schemas/intblocks.schema.json
  - Each property includes a description field for agents and tooling
- Build manifests: data/datasets/countries.manifest.json, intblocks.manifest.json, blocktypes.manifest.json
  - Fields: version, build_date, git_commit, row_count, schema_hash, data_license
- Parquet sidecars: data/datasets/{countries,intblocks,blocktypes}.meta.json
- ID remaps: data/datasets/intblocks_aliases.json (and .parquet)
- Country code remaps: data/datasets/countries_aliases.json
- Attribute intblock retirements → country fields: data/datasets/attribute_intblock_migrations.json
- Tool schemas: data/schemas/country_lookup.openai.json, intblock_lookup.openai.json
- Data dictionary: docs/data-dictionary.md
- Getting started: docs/getting-started.md

## Token budgets (approximate)

| Artifact | ~Tokens | Use |
|----------|--------:|-----|
| llms.txt (this file) | ~1.5k | Always load first |
| countries-lite.parquet (~12 cols × 256) | ~8–15k | Entity linking |
| intblocks-lite.parquet | ~40–80k | Org discovery |
| Full countries.jsonl | large | Avoid in-context; query DuckDB |
| Full intblocks with includes | very large | Prefer memberships table + filters |

Prefer DuckDB SQL or lite Parquet over stuffing full JSONL into the prompt.

Check schema_hash before upgrading downstream pipelines. Breaking changes are in CHANGELOG.md.

## Join keys

- Countries: code (ISO alpha-2, primary), iso3code (alpha-3), numeric_code, wikidata_id (Q-number)
- Intblocks: id (uppercase string, e.g. EU, NATO, UN)
- Intblock membership: includes[].id → country code (authoritative); includes[].name is a display label only
- Intblock taxonomy: blocktype (list of blocktype keys), partof (parent org ids)
- Blocktypes: id matches intblocks.blocktype values

## Critical gotchas

- borders uses ISO alpha-3 codes, NOT alpha-2
- population, area, gini are structs: {value, year, source, source_id} — use .value for the number
- Current ISO countries only (249): filter code_status == 'official_iso3166_1'
- 7 non-standard country codes retained with explicit status: AN, JG, XK, XA, XS, XT, XN (Kosovo is XK; legacy KV in countries_aliases.json)
- Country code remaps: data/datasets/countries_aliases.json
- World Bank region/incomeLevel/lendingType missing for 8 entities (territories WB does not classify); adminregion missing for 39 high-income economies by design
- Filter World Bank region on region.id (e.g. 'ECS'), not region.value: labels vary upstream ('(all income levels)' suffix)
- Intblock id renames: consult intblocks_aliases.json; reason=disambiguated means old id now refers to a different entity
- Attribute partitions (traffic hand, DVD region, scripts, legal/broadcast systems, rail gauges) are country fields — not intblocks; remap retired ids via attribute_intblock_migrations.json
- Driving side: filter countries.car_side ('left'|'right'); do not join traffichand intblocks (retired)
- scope_category filters entity kind: igo | treaty_body | policy_forum | reference_enumeration (geographic/set groupings only; see docs/intblock-inclusion-policy.md)
- timezones: empty for uninhabited territories; timezone_status may be 'not_applicable'

## Example queries (DuckDB)

SELECT dataset, version, schema_hash FROM _meta;

SELECT code, name FROM countries WHERE un_member = true;

SELECT code, name FROM countries WHERE code_status = 'official_iso3166_1';

SELECT code, name FROM countries WHERE car_side = 'left' ORDER BY code;

SELECT n.code, n.name FROM countries th, UNNEST(th.borders) AS b(iso3) JOIN countries n ON n.iso3code = b.iso3 WHERE th.code = 'TH';

SELECT i.id, i.name FROM intblocks i, UNNEST(i.includes) AS t(m) WHERE m.id = 'LA' AND m.type = 'country' LIMIT 10;

SELECT intblock_id FROM memberships WHERE country_code = 'LA' AND status = 'member' LIMIT 10;

SELECT id, name FROM intblocks WHERE list_contains(blocktype, 'intorg') LIMIT 10;

## Full documentation

- docs/agents/query.md — query and join workflow (all platforms)
- docs/agents/zh/query.md — 中文查询工作流（Kimi、GLM、通义灵码）
- docs/agents/contribute.md — YAML editing workflow (all platforms)
- docs/agents/zh/contribute.md — 中文 YAML 编辑工作流
- docs/agents/openspec-quickstart.md — schema change proposals
- docs/query-examples.md — verified DuckDB query cookbook (countries, borders, intblocks, joins)
- docs/llm-scenarios.md — intent-based copy/paste scenarios for LLM code generation
- docs/query-examples-polars.md — verified Polars / Parquet query cookbook
- docs/query-examples-r.md — verified R / dplyr / Parquet query cookbook
- docs/query-examples-observable.md — Observable Framework / Plot cookbook (DuckDB-Wasm)
- docs/query-examples.zh.md — 已验证中文场景查询
- docs/ai-consumers.md — agent-oriented data dictionary, recipes, scope
- llms.zh.txt — 中文紧凑索引
- AGENTS.zh.md — 中文智能体路由入口
- llms-full.txt — extended index for LLM crawlers
- AGENTS.md — root routing hub
- README.md — installation, build, schema tables, code examples
- docs/country-code-policy.md — non-standard codes, entity_type, filtering
- ATTRIBUTION.md — upstream sources and citation
- CONTRIBUTING.md — YAML authoring (for maintainers only)

## Extended index (llms-full only)

Everything above this line mirrors llms.txt; the sections below add detail for crawlers.

### Agent entry points by platform

- AGENTS.md — root routing hub (all platforms)
- AGENTS.zh.md — 中文智能体路由（Kimi、GLM、通义灵码）
- CLAUDE.md — Claude Code shim
- .github/copilot-instructions.md — GitHub Copilot shim
- .kimi/AGENTS.md — Kimi Code shim
- .lingma/rules/ — 通义灵码 Project Rules
- .cursor/skills/internacia-query/SKILL.md → docs/agents/query.md
- .cursor/skills/internacia-contribute/SKILL.md → docs/agents/contribute.md
- .agent/workflows/ — portable step-by-step workflows

### Additional data artifacts

- data/datasets/memberships.parquet — flattened country↔intblock edge table
- data/datasets/memberships.csv.zst — same edge table as CSV (zstd)
- data/datasets/countries.csv.zst / intblocks.csv.zst — flattened full tables
- data/datasets/countries-lite.parquet / intblocks-lite.parquet — identifier + classification columns
- data/datasets/datapackage.json — Frictionless resource index
- data/datasets/countries_aliases.json — country code remaps (KV→XK)
- data/datasets/attribute_intblock_migrations.json — retired attribute intblock → country field
- data/datasets/blocktypes.yaml — plain YAML copy of blocktypes (regenerated on build)
- data/datasets/intblocks_aliases.parquet — alias map in Parquet
- data/dataquality/ — data-quality analyzer reports

### Validation and completeness schemas

- data/schemas/countries.schema.json — countries JSON Schema
- data/schemas/intblocks.schema.json — intblocks JSON Schema
- data/schemas/countries_completeness.yaml — per-field null-rate gates (countries)
- data/schemas/intblocks_completeness.yaml — per-field null-rate gates (intblocks)
- data/schemas/wikidata_exclusions.yaml — allowed missing wikidata_id list
- scripts/validate_countries.py --json / scripts/validate_intblocks.py --json — structured validation output

### Maintainer documentation

- CONTRIBUTING.md — setup and PR checklist
- docs/enrichment.md — enrichment pipeline
- docs/getting-started.md — spreadsheet / DuckDB quick start
- docs/when-to-use-internacia.md — task-based routing guide for choosing Internacia
- docs/data-dictionary.md — generated field reference
- docs/architecture.md — pipeline diagram
- docs/intblock-inclusion-policy.md — scope_category taxonomy
- docs/entity-classification-policy.md — TW/PS/XK/EH edge cases
- docs/agents/contribute.md — YAML editing workflow
- docs/agents/openspec-quickstart.md — schema change proposals
- openspec/AGENTS.md — full OpenSpec reference
- ATTRIBUTION.md — upstream sources and citation formats
- CITATION.cff — machine-readable citation + Zenodo DOI
- CHANGELOG.md — release history and breaking changes
- docs/country-code-policy.md — non-standard codes, entity_type, un_status, filtering
